"use strict";
// Double numbers in an array:
// Making array of numbers:
let numbers = [1, 2, 3, 4, 5];
//Doubling each number:
let doubledNumbers = numbers.map((num) => num * 2);
// Printing new list of doubled numbers:
console.log(doubledNumbers);
