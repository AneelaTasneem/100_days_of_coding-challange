//Question 102: Calculate and log the absolute difference between the number -5 and 5.

// Define the two numbers
const number1 = -5;
const number2 = 5;

// Calculate the absolute difference between the two numbers
const absoluteDifference = Math.abs(number1 - number2);

// Log the absolute difference to the console
console.log(absoluteDifference);
