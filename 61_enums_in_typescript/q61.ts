//enums_in_typescript:

// Making enum(a list) for types of vehicles:

enum vehicle_type{
car,
motorbike,
truck
}

//Showing one of the vehicle from enum:
console.log(vehicle_type.truck); // it shows index number of truck i.e, no: 2